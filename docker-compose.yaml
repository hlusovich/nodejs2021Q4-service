version: "3"
volumes:
  applogs:
    external: true
  pstgdata:
    external: true
services:
  postgres:
    build: PostgressDocker
    restart: always
    env_file:
      - .env
    networks:
      - db
    ports:
      - ${POSTGRESS_PORT}:${POSTGRESS_PORT}
    environment:
      POSTGRES_DB: ${DB}
      POSTGRES_USER: ${SUPER_USER}
      POSTGRES_PASSWORD : ${POSTGRES_PASSWORD}
      POSTGRES_HOST_AUTH_METHOD: "trust"
      PGDATA: /var/lib/postgresql/data/my-data
    volumes:
      - pstgdata:/var/lib/postgresql/data

  node:
    build: .
    networks:
      - db
    volumes:
      - applogs:/srcOld/logs
    ports:
      - "${PORT}:${PORT}"
    restart: always
    depends_on:
      - postgres
networks:
  db:
    external:
      name: my-bridge

